1.  Function 1: 
    Set variable to a string.  
    Console log
    call next function with the variable as an argument.
    Output: "string"

2.  Function 2: 
    Set variable to a string
    Console log new string
    Call next function with variable as parameter
    Output: "string" + "newString"

3.  Function 3: 
    Console log Variable set in last function with the number 1 added to the end of the string
    Call next function with variable as a parameter
    Output: "newString" + 1

4.  Function 4: 
    If argument is less than 5 characters long console log true
    else console log false
    call next function as variable as parameter
    Output: If char !<5 = false
            If < 5 = true

5.  Function 5: 
    Create a loop that runs as many times as the length of the string
    Each run, console log a string